const id = (str) => str;
const result = id("Hello, World!");
console.log(result);
const getLength = (arr) => arr.length;
const arr = [4, 2, 6, 8, 1, 9, 0];
const res = getLength(arr);
console.log(res)